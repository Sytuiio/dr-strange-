language: python
dist: focal  
python:
  - "3.10"   

before_install:
  - sudo apt-get update

install:
  - python3 -m pip install --upgrade pip
  - python3 -m pip install telebot flask
  - python3 -m pip install telebot

script:
  - chmod +x * && chmod +x bgmi && python3 m.py